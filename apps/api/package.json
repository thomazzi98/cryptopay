{
  "name": "@cryptopay/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --env-file-if-exists=../../.env dist/main.api.js",
    "dev": "node --env-file-if-exists=../../.env --watch src/main.api.ts"
  },
  "dependencies": {
    "@cryptopay/shared": "0.1.0",
    "fastify": "5.12.3",
    "pino": "10.3.1",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "24.13.3"
  }
}
